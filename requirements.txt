# Runtime dependencies actually needed by the current phase.
# The full stack is locked in ADR-0001; each phase adds its own packages here
# as it starts, rather than installing months-early against versions that will
# have drifted by the time they're actually used.

Flask==3.0.3
python-dotenv==1.0.1

# Phase 1 — Core Data Model & Auth (ADR-0001 stack, ADR-0016 schema, ADR-0008 auth)
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
SQLAlchemy==2.0.35
alembic==1.19.2

# Phase 2 — Inventory & Product Management (ADR-0010 labels, ADR-0018 codes)
python-barcode==0.15.1
Pillow==10.4.0
reportlab==4.2.2

# Phase 3 — POS & Billing (ESC/POS receipt printing; Dev Spec §11 Phase 3 step 5)
python-escpos==3.1

# Session 21 — shop timezone (ADR-0024). Windows ships no IANA zone database, so
# zoneinfo cannot resolve Asia/Karachi in the packaged installer without this.
tzdata==2026.4

# Phase 5 — WhatsApp notifications (ADR-0001 background jobs, ADR-0031 scheduling).
# The Meta adapter uses the standard library's urllib (no HTTP dependency to bundle),
# and Windows DPAPI is reached through ctypes (ADR-0033) — no pywin32.
APScheduler==3.11.3

# Phase 7 — Windows packaging (ADR-0035 §7): the production web server. Pure Python, no
# compiler, Windows-supported; Flask's development server never ships.
waitress==3.0.2

# CSRF protection on every form (context.md §4, before Sukoon is on the shop network).
# WTF_CSRF_TIME_LIMIT is None in config: the default one-hour expiry would fail a sale on a
# till screen left open over lunch.
Flask-WTF==1.3.0

# Phase 7 — offsite backup (ADR-0037 §1): AES-256-GCM, so the storage provider holds bytes it
# cannot read. S3 requests are signed with the standard library (hmac + urllib), as the WhatsApp
# adapter does — no HTTP dependency to bundle.
cryptography==50.0.1
